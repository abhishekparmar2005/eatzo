{
  "name": "eatzo",
  "version": "1.0.0",
  "description": "Eatzo Food Delivery App - Root",
  "scripts": {
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm start",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "seed": "cd backend && node seed.js"
  }
}
